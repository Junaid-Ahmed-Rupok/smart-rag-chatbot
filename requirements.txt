# ── Web framework ─────────────────────────────────────────────────────────────
streamlit>=1.32.0,<2.0.0

# ── LangChain ─────────────────────────────────────────────────────────────────
langchain>=0.2.0,<0.3.0
langchain-community>=0.2.0,<0.3.0
langchain-core>=0.2.0,<0.3.0          # transitive but pin to avoid resolver drift

# ── Chat model (Groq — hosted, no install required) ──────────────────────────
langchain-groq>=0.1.5,<0.2.0
groq>=0.9.0                            # Python client; used directly for friendlier error types

# ── Embeddings (always local — Groq has no embeddings endpoint) ──────────────
sentence-transformers>=2.7.0,<6.0.0    # pulls in torch; first run downloads the model (~80MB)

# ── Vector store ──────────────────────────────────────────────────────────────
faiss-cpu>=1.8.0,<2.0.0

# ── Document loaders ──────────────────────────────────────────────────────────
pypdf>=4.0.0,<5.0.0                    # PyPDFLoader; pypdf 3.x has known extraction bugs
python-docx>=1.1.0,<2.0.0             # Docx2txtLoader dependency
docx2txt>=0.8                          # required by langchain Docx2txtLoader at runtime

# ── HTTP ──────────────────────────────────────────────────────────────────────
requests>=2.32.0,<3.0.0

# ── Config / env ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0

# ── Ollama integration (optional — only needed if LLM_PROVIDER=ollama) ───────
ollama>=0.1.8                          # Python client (langchain-community pulls this)

# ── Dev / optional ────────────────────────────────────────────────────────────
# Uncomment to enable:
# pytest>=8.0.0                        # unit tests
# pytest-asyncio>=0.23.0              # async test support
# ruff>=0.4.0                         # linter
